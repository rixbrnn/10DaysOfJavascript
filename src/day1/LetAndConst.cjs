// Write your code here. Read input using 'readLine()' and print output using 'console.log()'.


// NOTE: insteaed of using readLine we're using the input parameter, change it to readLine() to use the console input
// on the hackerrank exercise (or just uncomment the following code, and comment the other one)
// const radius = readLine();
const radius = "2.6";
console.log(radius);

const PI = Math.PI;

// Print the area of the circle:
console.log(PI * radius * radius);

// Print the perimeter of the circle:
console.log(2 * PI * radius);
